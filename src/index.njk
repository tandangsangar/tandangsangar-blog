---
layout: layouts/base.njk
title: Home
---

<div class="bg-white mx-3 md:mx-6 border-2 border-black border-b-0">
    
    {% set textAtas = global.marquee_top %}
    
    <div class="w-full bg-[#ccff00] border-b-2 border-black py-2 overflow-hidden relative z-20 flex">
        <div class="animate-marquee flex flex-shrink-0 items-center whitespace-nowrap min-w-full">
            <span class="mx-4 font-bold text-lg uppercase font-header">{{ textAtas }}</span>
            <span class="mx-4 font-bold text-lg uppercase font-header">{{ textAtas }}</span>
            <span class="mx-4 font-bold text-lg uppercase font-header">{{ textAtas }}</span>
            <span class="mx-4 font-bold text-lg uppercase font-header">{{ textAtas }}</span>
        </div>

        <div class="animate-marquee flex flex-shrink-0 items-center whitespace-nowrap min-w-full">
            <span class="mx-4 font-bold text-lg uppercase font-header">{{ textAtas }}</span>
            <span class="mx-4 font-bold text-lg uppercase font-header">{{ textAtas }}</span>
            <span class="mx-4 font-bold text-lg uppercase font-header">{{ textAtas }}</span>
            <span class="mx-4 font-bold text-lg uppercase font-header">{{ textAtas }}</span>
        </div>
    </div>

    <section class="relative w-full h-[90vh] min-h-[600px] bg-black border-b-2 border-[#f0f0f0] overflow-hidden select-none flex flex-col justify-between group">
    
    <div class="absolute inset-0 z-0">
        {% if global.hero_image %}
        <img src="{{ global.hero_image }}" alt="Hero Background" class="w-full h-full object-cover opacity-100 transition-all duration-1000 group-hover:scale-110">
        {% endif %}
        
        <div class="absolute inset-0 opacity-20" 
             style="background-image: linear-gradient(#333 1px, transparent 1px), linear-gradient(90deg, #333 1px, transparent 1px); background-size: 40px 40px;">
        </div>
    </div>

    <div class="relative z-10 w-full pointer-events-none">
        <h1 class="font-header text-[22vw] leading-[0.75] text-[#f0f0f0] uppercase tracking-tighter -ml-[3vw] -mt-[2vw] mix-blend-exclusion">
            {{ global.hero_text_solid }}
        </h1>
    </div>

    <div class="absolute z-50 w-full max-w-sm px-4 top-1/2 -translate-y-1/2 
                left-1/2 -translate-x-1/2 
                md:left-auto md:right-[15%] md:translate-x-0">
        
        <div class="bg-[#f0f0f0] p-6 border-2 border-black shadow-[8px_8px_0px_0px_#000] rotate-2 hover:rotate-0 transition-transform duration-300">
            <div class="flex items-center gap-2 mb-4 border-b-2 border-black pb-2">
                <span class="text-2xl animate-pulse">⚡</span>
                <h2 class="font-header text-2xl uppercase tracking-wide text-black">
                    {{ global.manifesto_title }}
                </h2>
            </div>
            <p class="font-mono text-sm leading-relaxed mb-6 uppercase text-black">
                {{ global.manifesto_text }}
            </p>
            <a href="#articles" class="group/btn block w-full border-2 border-black bg-white p-3 font-bold font-header uppercase text-lg hover:bg-black hover:text-[#ccff00] transition-colors flex justify-between items-center text-black">
                {{ global.manifesto_btn }}
                <span class="group-hover/btn:translate-x-1 transition-transform">&rarr;</span>
            </a>
        </div>
    </div>

    <div class="relative z-10 w-full flex flex-col items-end justify-end pb-[40px]">
        
        <h1 class="font-header text-[20vw] leading-[0.75] text-transparent uppercase tracking-tighter text-right w-full pointer-events-none -mr-[3vw] relative z-0"
            style="-webkit-text-stroke: 2px #f0f0f0;">
            {{ global.hero_text_outline }}
        </h1>

        {% set textBawah = global.marquee_bottom %}
        <div class="absolute bottom-0 left-0 w-full bg-[#f0f0f0] border-t-2 border-black py-2 z-30 flex overflow-hidden">
             <div class="animate-marquee-reverse flex flex-shrink-0 items-center whitespace-nowrap min-w-full">
                <span class="mx-8 text-black text-lg md:text-xl font-mono font-bold">{{ textBawah }}</span>
                <span class="mx-8 text-black text-lg md:text-xl font-mono font-bold">{{ textBawah }}</span>
                <span class="mx-8 text-black text-lg md:text-xl font-mono font-bold">{{ textBawah }}</span>
                <span class="mx-8 text-black text-lg md:text-xl font-mono font-bold">{{ textBawah }}</span>
                <span class="mx-8 text-black text-lg md:text-xl font-mono font-bold">{{ textBawah }}</span>
            </div>
            <div class="animate-marquee-reverse flex flex-shrink-0 items-center whitespace-nowrap min-w-full">
                <span class="mx-8 text-black text-lg md:text-xl font-mono font-bold">{{ textBawah }}</span>
                <span class="mx-8 text-black text-lg md:text-xl font-mono font-bold">{{ textBawah }}</span>
                <span class="mx-8 text-black text-lg md:text-xl font-mono font-bold">{{ textBawah }}</span>
                <span class="mx-8 text-black text-lg md:text-xl font-mono font-bold">{{ textBawah }}</span>
                <span class="mx-8 text-black text-lg md:text-xl font-mono font-bold">{{ textBawah }}</span>
            </div>
        </div>
    </div>

</section>

    <section id="articles" class="w-full">
        <div class="grid grid-cols-1 md:grid-cols-4 auto-rows-[300px] md:auto-rows-[350px] gap-0 border-b-2 border-black bg-black grid-flow-dense">
            
            {% for post in collections.posts | reverse %}
                {% set i = loop.index0 % 12 %}
                {% set spanClass = "md:col-span-1 md:row-span-1" %}

                {% if i == 0 or i == 4 or i == 10 %}
                    {% set spanClass = "md:col-span-2 md:row-span-2" %}
                {% elif i == 1 or i == 7 or i == 11 %}
                    {% set spanClass = "md:col-span-2 md:row-span-1" %}
                {% elif i == 2 or i == 5 or i == 8 %}
                    {% set spanClass = "md:col-span-1 md:row-span-2" %}
                {% endif %}

                <a href="{{ post.url }}" class="group relative block overflow-hidden border border-black bg-black {{ spanClass }}">
                    <div class="absolute inset-0 w-full h-full z-0">
                         <img src="{{ post.data.image or 'https://placehold.co/600x400/1a1a1a/FFF?text=NO+IMG' }}" 
                            alt="{{ post.data.title }}" 
                            class="w-full h-full object-cover grayscale opacity-70 group-hover:grayscale-0 group-hover:opacity-100 transition-all duration-700">
                    </div>
                    <div class="absolute inset-0 z-10 bg-gradient-to-t from-black/90 via-black/10 to-transparent pointer-events-none"></div>

                    <div class="relative z-20 h-full flex flex-col justify-between p-4 md:p-6">
                        <div class="self-start">
                          <span class="inline-block px-2 py-1 border border-black bg-[#ccff00] text-black font-mono text-[10px] md:text-xs font-bold uppercase shadow-[2px_2px_0px_#000]">
                            {{ post.data.category or 'UNCATEGORIZED' }}
                          </span>
                        </div>

                        <div class="translate-y-2 group-hover:translate-y-0 transition-transform duration-300">
                          <h3 class="font-header text-2xl md:text-3xl lg:text-4xl leading-[0.9] text-white uppercase mb-2 drop-shadow-[2px_2px_0px_#000]">
                            {{ post.data.title }}
                          </h3>
                          <div class="flex items-center gap-2 font-mono text-[10px] md:text-xs text-[#ccff00] font-bold uppercase opacity-60 group-hover:opacity-100 transition-opacity duration-300">
                             <span>{{ post.date | date("DD MMM YYYY") }}</span>
                             <span class="text-white">///</span>
                             <span>{{ post.data.author }}</span>
                          </div>
                        </div>
                    </div>
                </a>
            {% else %}
                <div class="col-span-full md:row-span-2 py-20 bg-[#f0f0f0] flex flex-col items-center justify-center text-center text-black">
                    <p class="font-header text-2xl uppercase mb-2">NO SIGNALS FOUND</p>
                    <p class="font-mono text-sm">We haven't documented this sector yet.</p>
                </div>
            {% endfor %}

        </div>
    </section>
</div>

<div class="h-4 md:h-8"></div>

<div class="bg-[#ccff00] mx-3 md:mx-6 border-2 border-black">
  <section class="w-full py-16 md:py-24 px-4 md:px-12 flex flex-col items-center justify-center relative overflow-hidden">
    <div class="absolute top-0 left-0 w-full h-full overflow-hidden opacity-10 pointer-events-none select-none flex items-center justify-center">
         <h1 class="text-[20vw] font-header font-black text-black leading-none">SUBSCRIBE</h1>
    </div>
    
    <div class="flex flex-col lg:flex-row items-end lg:items-center justify-between gap-12 max-w-7xl mx-auto w-full relative z-10">
      <div class="lg:w-1/2 text-left w-full">
        <h2 class="text-6xl md:text-8xl font-header uppercase leading-[0.85] mb-6 text-black">
          DON'T MISS <br/>THE CHAOS
        </h2>
        <p class="font-mono font-bold text-base md:text-lg max-w-md border-l-4 border-black pl-4 py-1 text-black">
          Join the collective. We spam maybe once a week. No corporate bs.
        </p>
      </div>
      <div class="lg:w-1/2 w-full flex flex-col md:flex-row gap-0 items-stretch brutal-shadow">
        <input placeholder="YOUR@EMAIL.COM" class="flex-grow border-2 border-black p-6 font-mono text-lg focus:outline-none focus:bg-black focus:text-[#ccff00] transition-colors placeholder:text-gray-500 text-black">
        <button class="bg-black text-white px-8 py-4 font-bold font-header text-xl uppercase border-2 border-black hover:bg-white hover:text-black transition-all">
          SUBSCRIBE
        </button>
      </div>
    </div>
  </section>
</div>

<div class="h-24 md:h-12 w-full flex justify-center">
    <div class="h-full w-2 "></div>
</div>