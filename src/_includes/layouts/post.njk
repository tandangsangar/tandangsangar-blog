---
layout: layouts/base.njk
---

<article class="min-h-screen border-b-2 border-black bg-white font-mono">
    
    <div class="flex flex-col md:flex-row">
        
        <aside class="w-full md:w-4/12 border-b-2 md:border-b-0 md:border-r-2 border-black bg-[#f0f0f0] md:h-screen md:sticky md:top-0 flex flex-col z-20">
            
            <div class="p-6 md:p-8">
                <a href="/" class="inline-block text-xs font-bold underline underline-offset-4 decoration-2 decoration-black hover:bg-black hover:text-[#ccff00] transition-colors mb-8">
                    &larr; BACK TO ARCHIVE
                </a>
                
                <div>
                    <span class="bg-black text-[#ccff00] px-2 py-1 text-xs font-bold uppercase border border-black inline-block mb-2">
                        {{ category }}
                    </span>
                    <h1 class="font-header text-5xl md:text-6xl lg:text-7xl uppercase leading-[0.85] tracking-tight break-words">
                        {{ title }}
                    </h1>
                </div>
            </div>

            <div class="px-6 md:px-8 py-4 flex-grow">
                <div class="border-l-2 border-black pl-4 space-y-4">
                    <div>
                        <p class="text-[10px] text-gray-500 uppercase tracking-widest mb-1">Written / Curated by</p>
                        <div class="flex items-center gap-2">
                            <div class="w-8 h-8 bg-black rounded-full border border-white overflow-hidden">
                                <div class="w-full h-full flex items-center justify-center text-white text-xs">
                                    {{ author | truncate(1, true, "") }}
                                </div>
                            </div>
                            <span class="font-bold uppercase text-sm">{{ author }}</span>
                        </div>
                    </div>

                    <div>
                        <p class="text-[10px] text-gray-500 uppercase tracking-widest mb-1">Published On</p>
                        <p class="font-bold text-sm">{{ date | date("DD MMM YYYY") }}</p>
                    </div>

                    <div>
                         <p class="text-[10px] text-gray-500 uppercase tracking-widest mb-1">Tags</p>
                         <div class="flex flex-col gap-1 items-start">
                            {% for tag in tags %}
                                {% if tag != "posts" %}
                                <span class="text-xs before:content-['#'] opacity-70 hover:opacity-100 cursor-pointer">{{ tag }}</span>
                                {% endif %}
                            {% endfor %}
                         </div>
                    </div>
                </div>
            </div>

            <div class="p-6 md:p-8 mt-auto border-t-2 border-black bg-white">
                <button class="w-full border-2 border-black bg-white py-3 font-header text-xl uppercase hover:bg-black hover:text-white hover:translate-x-[2px] hover:translate-y-[2px] hover:shadow-none shadow-[4px_4px_0px_0px_rgba(0,0,0,1)] transition-all flex items-center justify-center gap-2 group">
                    <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="group-hover:stroke-white"><circle cx="18" cy="5" r="3"/><circle cx="6" cy="12" r="3"/><circle cx="18" cy="19" r="3"/><line x1="8.59" y1="13.51" x2="15.42" y2="17.49"/><line x1="15.41" y1="6.51" x2="8.59" y2="10.49"/></svg>
                    SHARE THIS
                </button>
            </div>

        </aside>

        <div class="w-full md:w-8/12 bg-white">
            
            <div class="w-full aspect-video border-b-2 border-black overflow-hidden relative group">
                <img src="{{ image }}" alt="{{ title }}" class="w-full h-full object-cover grayscale contrast-125 group-hover:grayscale-0 transition-all duration-700">
                <div class="absolute inset-0 bg-[url('data:image/svg+xml;base64,PHN2ZyB4bWxucz0iaHR0cDovL3d3dy53My5vcmcvMjAwMC9zdmciIHdpZHRoPSI0IiBoZWlnaHQ9IjQiPgo8cmVjdCB3aWR0aD0iNCIgaGVpZ2h0PSI0IiBmaWxsPSIjZmZmIi8+CjxyZWN0IHdpZHRoPSIxIiBoZWlnaHQ9IjEiIGZpbGw9IiMwMDAiIG9wYWNpdHk9IjAuMSIvPgo8L3N2Zz4=')] opacity-20 pointer-events-none"></div>
            </div>

            <div class="p-6 md:p-16 lg:p-20">
                
                <p class="text-xl md:text-2xl font-serif italic leading-relaxed mb-12 border-l-4 border-[#ccff00] pl-6 py-2">
                    {{ excerpt }}
                </p>

                <div class="prose prose-lg max-w-none 
                            prose-headings:font-header prose-headings:uppercase prose-headings:tracking-tight
                            prose-p:font-serif prose-p:text-lg prose-p:leading-8 prose-p:text-gray-800
                            prose-a:text-black prose-a:bg-[#ccff00] prose-a:no-underline prose-a:px-1 prose-a:border prose-a:border-black hover:prose-a:bg-black hover:prose-a:text-[#ccff00]
                            prose-blockquote:border-l-4 prose-blockquote:border-black prose-blockquote:pl-4 prose-blockquote:italic
                            prose-img:border-2 prose-img:border-black prose-img:shadow-[4px_4px_0px_0px_rgba(0,0,0,1)]
                            prose-ul:list-square prose-li:marker:text-[#ccff00]">
                    
                   {{ content | safe }}
                
                </div>
            </div>

            <div class="border-t-2 border-black bg-[#f0f0f0] p-8 md:p-12">
                <h3 class="font-header text-3xl uppercase mb-6 flex items-center gap-2">
                    MORE FROM <span class="bg-black text-white px-2">{{ category }}</span>
                </h3>
                
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    {% set relatedPosts = collections.posts | filterByCategory(category) %}
                    {% set count = 0 %}
                    
                    {% for post in relatedPosts | reverse %}
                        {% if post.url != page.url and count < 2 %}
                            <a href="{{ post.url }}" class="group block bg-white border-2 border-black hover:shadow-[4px_4px_0px_0px_rgba(0,0,0,1)] hover:-translate-y-1 transition-all">
                                <div class="h-40 overflow-hidden border-b-2 border-black relative">
                                    <img src="{{ post.data.image }}" class="w-full h-full object-cover grayscale group-hover:grayscale-0 transition-all">
                                </div>
                                <div class="p-4">
                                    <h4 class="font-header text-xl uppercase leading-none group-hover:text-[#ccff00] group-hover:bg-black inline-block transition-colors">
                                        {{ post.data.title }}
                                    </h4>
                                </div>
                            </a>
                            {% set count = count + 1 %}
                        {% endif %}
                    {% endfor %}
                    
                    {% if count == 0 %}
                         <div class="border-2 border-dashed border-gray-400 p-6 flex items-center justify-center">
                            <span class="font-mono text-xs text-gray-500">NO SIGNAL</span>
                         </div>
                    {% endif %}
                </div>
            </div>

        </div>
    </div>
</article>

<div class="bg-[#ccff00] border-b-2 border-black">
  <section class="w-full py-16 px-4 md:px-12 flex flex-col items-center justify-center relative overflow-hidden">
    <div class="absolute top-0 left-0 w-full h-full overflow-hidden opacity-10 pointer-events-none select-none flex items-center justify-center">
         <h1 class="text-[10vw] font-header font-black text-black leading-none">JOIN US</h1>
    </div>
    <div class="flex flex-col lg:flex-row items-end lg:items-center justify-between gap-12 max-w-7xl mx-auto w-full relative z-10">
      <div class="lg:w-1/2 text-left w-full">
        <h2 class="text-5xl md:text-7xl font-header uppercase leading-[0.85] mb-4 text-black">
          KEEP IT <br/>LOUD
        </h2>
        <p class="font-mono font-bold text-sm md:text-base border-l-4 border-black pl-4 py-1 text-black">
          Subscribe for weekly updates. No spam, just pure noise.
        </p>
      </div>
      <div class="lg:w-1/2 w-full flex flex-col md:flex-row gap-0 items-stretch brutal-shadow">
        <input placeholder="YOUR@EMAIL.COM" class="flex-grow border-2 border-black p-4 font-mono text-base focus:outline-none focus:bg-black focus:text-[#ccff00] transition-colors text-black placeholder:text-gray-500">
        <button class="bg-black text-white px-6 py-4 font-bold font-header text-lg uppercase border-2 border-black hover:bg-white hover:text-black transition-all">
          SUBMIT
        </button>
      </div>
    </div>
  </section>
</div>

<div class="h-24 w-full flex justify-center bg-white">
    <div class="h-full w-2 bg-black"></div>
</div>